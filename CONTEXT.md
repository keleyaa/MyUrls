# MyURL 领域上下文

## 术语

- **短链（Short Link）**：服务公开给访问者的短入口。访问短链会解析到一个目标地址。
- **短码（Code）**：短链路径中的公开标识，用于定位短链记录。自动生成的短码不承载业务含义。
- **别名（Alias）**：由创建者主动指定的短码。别名是公开路径，不应承载密码或其他秘密。
- **目标地址（Target URL）**：短链解析后跳转到的绝对 HTTP(S) 地址。
- **创建（Create）**：校验目标地址和可选别名、通过滥用防护后，为目标地址分配短码并返回短链。
- **解析（Resolve）**：根据短码查找目标地址并执行 HTTP 重定向。不存在或已过期的短链无法解析。
- **过期（Expiration）**：短链停止可解析的时间点。当前产品规则是创建后固定 90 天。
- **挑战（Challenge）**：风险较高的创建请求需要完成的人机验证。当前使用 Turnstile 作为验证提供方。
- **风险分（Risk Score）**：针对创建来源累计的滥用风险量化结果，用于决定放行、挑战或拒绝。
- **客户端指纹（Client Fingerprint）**：由服务端密钥和客户端网络地址计算出的限流标识，不是原始网络地址。
- **短链记录（Link Record）**：关联短码、目标地址和过期时间的业务记录。

## 核心关系

- 一个短链记录有且只有一个短码、一个目标地址和一个过期时间。
- 一个短码在其有效期内最多对应一个短链记录。
- 别名参与短码分配，因此已被占用的别名不能再次创建。
- 解析只返回仍处于有效期内的短链记录；过期后的记录不再对外可见。
